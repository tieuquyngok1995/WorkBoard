export enum ProgramMode {
    CREATE,
    EDIT,
    READ
}

export enum TaskPriority {
    HIGH,
    MEDIUM,
    LOW
}

export enum TaskType {
    CODING,
    TESTING,
    REVIEW,
    FIXBUG
}

export enum JobStatus {
    WAITING,
    PROGRESS,
    PENDING,
    DONE
}