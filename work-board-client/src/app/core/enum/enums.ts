export enum ProgramMode {
    INIT,
    CREATE,
    EDIT,
    READ,
    LIST
}

export enum TaskPriority {
    HIGH,
    MEDIUM,
    LOW
}

export enum TaskType {
    CODING,
    TESTING,
    REVIEW,
    FIXBUG
}